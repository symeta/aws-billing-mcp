{
  "name": "aws-billing-mcp",
  "version": "2.0.0",
  "description": "MCP server exposing AWS Billing and Cost Management data via the AWS CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "aws-billing-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "tsx src/test-client.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
